# 컨테이너 운반비 견적 챗봇 — 인수인계

> 새 채팅에서 이어갈 때 이 파일과 `컨테이너_운반비_견적.html`을 같이 올려주세요.

---

## 1. 무엇을 만들고 있나

국양로지텍(KYL) **내부 직원용** 컨테이너 운반비 견적 챗봇.
2026년 화물자동차 안전운임 고시(국토교통부고시 2026-55호, 시행 2026.2.1)를 그대로 계산합니다.
단일 HTML 파일이며 인터넷 없이도 동작합니다.

**결정된 사항**
- 사용자: 내부 직원 → 안전위탁 / 운수사업자간 / 안전운송 **3개 운임 전부 표시**
- 입력: **대화형**. 한 줄 문장으로 던지면 견적이 바로 나옴
- 모바일 지원 (하단 시트 방식)
- 법정동 1:N → **자동선택 금지, 반드시 물어봄** (초기엔 "차이 작으면 자동"이었으나 폐기)

---

## 2. 받은 자료 (모두 검증 완료)

| 파일 | 내용 |
|---|---|
| `_전문__2026년_적용_화물자동차_안전운임_고시.pdf` | 고시 전문. 거리별 운임표 550구간 + 별표1 부대조항 |
| `_별첨__…_기점별_운임_왕복_.xlsx` | 기점 14개 × 전국 행정동 3,480개 왕복 운임 |
| `_별첨__…_기점별_운임_편도_.xlsx` | 부산신항/부산북항/광양항 ↔ 의왕ICD 편도, 수도권 1,213개 |
| `jscode20260701_말소코드포함_.zip` | 행안부 행정표준코드. **KIKmix**가 행정동↔법정동 매핑 |
| `국가데이터처_법정동_연계정보_20250602.csv` | 2013~2025 48개 분기 스냅샷 (104만행). 폐지 행정동 이력 |

**카카오 REST API 키**: `afb3dc1b4837efa16dd2d4628e27e133`

---

## 3. 검증으로 밝혀낸 것 (중요)

### 3-1. 운임표 = 거리표에서 유도됨
- **10개 기점**(부산신항·북항, 광양, 울산신항·항, 포항, 군산, 마산, 대산, 의왕ICD) → 거리별 운임표와 **100% 일치**
- **4개 기점**(인천신항·인천항·인천국제여객터미널·평택항) → **할증이 이미 반영된 별도 표**
  - 인천 = 안전위탁운임 ×1.2, 평택 = ×1.18 (검증 완료)
  - → **이 표를 쓸 땐 인천/평택 기점할증을 다시 더하면 안 됨**
  - 거리만의 함수임을 확인 → 거리→운임 테이블로 압축 가능
- **편도표**는 거리만으로 결정 안 됨(같은 거리에 값 여러 개) → 행선지별 값 저장 필요
  - 편도 거리 = (기점→화주) + (화주→의왕ICD), 전 행 검증 완료

### 3-2. 행정동 매핑 (미매칭 0건 달성)
운임표는 **행정동** 기준. 단순 조인하면 858건 깨짐. 정규화 규칙 5개로 해결:

| 문제 | 예 | 처리 |
|---|---|---|
| 전남·광주 통합 | 코드파일 `전남광주통합특별시` | 시군구로 분기(동·서·남·북·광산구 → 광주) |
| 화성시 구 설치 | `화성시 동탄구` 등 | 구 접기 → `화성시` |
| "제" 표기 | `묵제1동`, `거제제1동` | 숫자 앞 '제' 1개만 제거 |
| 세종 단층제 | 시군구 공란 | `세종시`로 채움 |
| 가운뎃점 | `종로1·2·3·4가동` | `·` `.` 제거 |
| 폐지 행정동 51개 | 안양8·9동, 동탄1~9동 | 말소코드 + 국데처 이력으로 커버 |

**두 소스를 합쳐야 미매칭 0**:
- KIKmix 단독 → 1건 누락 / 국데처 최신 단독 → 14건 / 국데처 전이력 → 7건 / **합산 → 0건**

**결과**: 법정동 20,549개 중 19,152개(93.2%) 1:1, 1,397개(6.8%) 1:N

---

## 4. 앱 구조

`컨테이너_운반비_견적.html` — 단일 파일 (약 780KB), 데이터 임베드

### 데이터 (base36 압축)
- `DONGS` 3,480 행선지 / `DIST` 기점별 구간거리 / `RATE` 거리표 550구간
- `SUR` 인천3+평택 전용표 / `ONE` 편도표 / `BJ` 법정동→행정동 / `COV` 행정동→관할법정동

### 주소 → 행정동 (2단계)
1. **카카오 API** (온라인): 주소검색 → 좌표 → `coord2regioncode` → 행정동(H) → 자동 확정
2. **내장 주소록** (오프라인 폴백): 시·군·구 먼저 확정 → 동 이름 **토큰 완전일치**
   - ※ 부분문자열 매칭 금지. '서정동'이 '정동'에 걸려 소공동이 뜨던 버그 있었음

### 계산 순서
```
기본운임 조회(왕복/편도표) → 규격배수(45FT=112.5%, COMBINE) → 형태배수(복화·밥테일·셔틀…)
→ 할증 합산(최고 1개 전액 + 나머지 각 50%, 최대 3개 / 별표1 22)
→ 십원단위 반올림 → ×수량 → 부대비용(대기료·세척·X-ray·통행료 실비) → VAT 별도
```

### 자연어 파서
문장에서 기점 / 규격(40FT·20피트·45·콤바인) / 수량(2대) / 톤(25톤) / 형태(복화·밥테일·편도) /
할증(냉동·탱크·위험물·심야·공휴일·험로…) 추출, 나머지 → 행선지.
빠진 것만 되묻고, 견적 후에도 문장으로 조건 추가 가능(`심야 냉동 3대`).

---

## 5. 지금 상태 / 남은 일

### ✅ 동작
- 기점 14개, 전국 행정동 3,480개, 왕복·편도, 규격·형태·할증·부대비용, 근거 표시, 모바일

### ⚠️ 미해결
1. **카카오 API 미연결** — `file://`로 열면 브라우저가 차단.
   → `python -m http.server 8000` 또는 사내 서버 배포 + 카카오 콘솔 **플랫폼 > Web** 도메인 등록
2. **전배 운송 요율표** (별표1 18.라 "별첨") — 자료 미수령
3. **미구현 항목**: 배차취소료(별표1 26), 과적단속(31), 공차운행(14) — 사후정산이라 제외
4. **해석 확인 필요**: 할증을 "기본운임 × 형태배수"에 적용 중.
   복화·밥테일처럼 배수 붙는 경우 사내 관행과 다를 수 있음

---

## 6. 검산용 정답

**평택항 → 평택시 서정동** (법정동 서정동 = 행정동 중앙동/서정동 2곳에 걸침)

| 행정동 | 거리 | 40FT 위탁 | 40FT 사업자간 | 40FT 운송 |
|---|---|---|---|---|
| 중앙동 | 29km | 248,800 | 263,400 | **278,600** |
| 서정동 | 34km | 266,400 | 281,800 | **297,800** |

(평택항 18% 할증 반영, 왕복 1대, 부가세 별도)
