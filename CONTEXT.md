# 배차일보 웹앱 — 프로젝트 컨텍스트

> 이 문서는 Claude 웹 대화에서 배차일보 엑셀과 KYSS 실적 엑셀을 분석한 결과를 정리한 인계 문서다.
> Claude Code 세션 시작 시 이 파일을 먼저 읽는다. 작업이 끝날 때마다 이 문서를 갱신한다.
>
> 작성일 2026-07-27 · 분석 대상 2026년 데이터

---

## 1. 무엇을 만드는가

**배차일보를 대체하는 웹앱.** 지금 엑셀(`26년배차일보.xlsx`)로 관리하는 배차 대장을 웹으로 옮긴다.

핵심 전제 — **이 앱은 KYSS를 대체하지 않는다. KYSS의 앞단이다.**

```
[배차 입력]  →  앱(배차일보)  →  마감  →  KYSS(실적·정산)
```

앱은 배차 예정 정보를 다루고, 확정 금액과 정산은 KYSS가 담당한다.
이 전제가 흔들리면 아래 설계가 전부 틀어진다.

### 요구사항 (사용자가 제시한 순서대로)

1. 엑셀을 네트워크·모바일에서 실시간 공유
2. 여러 명이 동시 사용
3. Firebase(Firestore + Hosting)로 구현
4. OCR — 메신저 텍스트를 필터 없이 붙여넣으면 자동 입력
5. D/O·PDF 읽기
6. 차량별·날짜별·화주별 요약
7. 엑셀 같은 필터
8. 숫자만 입력하면 차량번호 자동 완성
9. 화주명 입력 시 주소·연락처 자동 완성
10. 지역+화주 조합 검색 (`삼성청주`)
11. 컨테이너번호 오기입 표시

---

## 2. 기술 스택 (확정)

| 항목 | 선택 | 이유 |
|---|---|---|
| DB | Firestore | 실시간 푸시(onSnapshot), 폴링 불필요 |
| 호스팅 | Firebase Hosting | `https://프로젝트명.web.app`, 외부 폰 접속 가능 |
| 인증 | Firebase Auth | 관리자가 계정 발급, 가입 개방 안 함 |
| 서버 로직 | Cloud Functions | KYSS API 중계, Claude API 중계 |
| 요금제 | Spark → Blaze | Cloud Functions 쓰는 시점에 Blaze 전환 필요(카드 등록) |

- 회사가 Microsoft 365 비즈니스를 쓰지 않아 SharePoint는 탈락.
- Google Sheets도 검토했으나 Firebase가 우위.
- 무료 한도 여유 충분 — 월 850건, 하루 30~40건 수준.

### 보안 원칙 (반드시 지킬 것)

- **API 키·계정정보를 브라우저 코드에 절대 넣지 않는다.** 소스 보기 한 번이면 노출된다.
- KYSS 계정, Claude API 키는 Cloud Functions 환경변수(Secret Manager)에만 둔다.
- 개발 중에는 `functions/.env`, **`.gitignore`에 반드시 추가**.
- 금액 열은 별도 권한으로 분리 (기사·현장에는 배차 정보만).

---

## 3. 데이터 현황

### 3.1 배차일보 (`26년배차일보.xlsx` → 정리본 `26년배차일보_정리.xlsx`)

- 원본 6,793행 / 2025-12 ~ 2026-07
- **2025년분은 폐기 확정.** 2026년만 사용 → **5,935행**
- 시트 14개지만 실데이터는 `전체` 하나. 나머지는 전부 파생(미러·SUMIF 집계)

**정리본에서 이미 수정한 것**

| 항목 | 내용 |
|---|---|
| 날짜 | 12월분 858건이 2026-12로 잘못 입력 → 2025-12로 보정 |
| 날짜 | `-` 2건, `2/23` 1건 → 인접 원건 기준 보정 |
| 주소 | 시도명 통일(경기도→경기), 줄바꿈·공백 제거 3,072셀, 표기 통합 31건 |
| 규격 | 숫자 `20`/`45` → 문자 통일 2,192건 |
| 규격 | 40RF·40RH·20RF·20RH → RE로 통일 (18건), 20D → 20 (3건) |
| 열 추가 | Y=구분(자차/용차), Z=사이즈(20/40/45), AA=타입(D/H/RE/FR/OT) |
| 수식 | 월별 시트 SUMIF 271개 범위 고정, 깨진 외부링크 제거 |

정리 전후 청구·하불 합계는 완전히 동일하다(검증 완료).

### 3.2 KYSS 실적 (`실적통합관리_2026-07-27.xlsx` + `__1_.xlsx`)

- 두 파일은 **교집합 0**. 서로 다른 조각이다. 합쳐서 2,411행 (2026년만 2,410행)
- 같은 BL·같은 화주인데 컨테이너만 나뉘어 들어감 → 한쪽 추출에서 40건이 누락된 것으로 보임
- **평택사무소 / 구분=도어**만 담김

**열 15개**: 번호, 구분, 하불확정일, 배차일자, 화주, 차량번호, 차량구분, 매입처, 컨번호, 오더 청구처, 청구합계, 하불합계, 청구영업소, BL(BKG), 기사명

---

## 4. 반드시 알아야 할 함정 (여기서 실수했던 것들)

### 4.1 KYSS `청구합계`는 행별 금액이 아니다 ★★★

**컨테이너 단위 총액이 모든 행에 반복**된다. 하불만 행별 실제 금액이다.

```
SKLU1527256  두손  2026-02-02
  [일보] 다원 300,000 / 청명 210,000   → 합계 510,000
  [실적] 두 행 모두 510,000            → 단순 합산하면 1,020,000 (과다)
```

단순 SUM하면 **2,707만원 과다**. 컨번호+BL 단위로 중복 제거해야 한다.
KYSS API 응답도 같은 구조인지 반드시 확인할 것.

### 4.2 컨번호는 유일키로 쓸 수 없다

- 재사용 573건 (같은 컨테이너가 몇 달 뒤 재등장)
- 컨번호 없는 행 13건
- `BULK`, `LCL`, `베스트우암` 등 컨테이너 아닌 값 6건
- 한 셀에 여러 개(줄바꿈) 2건 — 둘 다 `공만차계근`, 금액 없음

→ **앱이 자체 배차번호를 발번**하고, 컨번호는 검색·대사용 보조키로 쓴다.

### 4.3 봉인번호 칸이 봉인번호가 아니다

실제 값 분포: `자가O` 1,655건, `통관O\n자가O` 228건, `자가접수중` 104건...
**자가/통관 상태값**이 대부분이고 진짜 봉인번호(`PCBJ-2609S`)는 소수.

→ 자가 체크 / 통관 체크 / 실봉인번호 **3개 필드로 분리**한다.

### 4.4 그 밖에 정의가 무너진 칸

| 칸 | 실제 내용 | 조치 |
|---|---|---|
| 보세근거번호 (39%) | `대기 45분 4만원`, `라운드`, 길안내 메모 | 메모로 정의 변경 |
| 테어 (41%) | `3700` 중량과 `상해` 지명이 섞임 | 숫자만 허용 |
| KM (7.8%) | 사실상 미사용 | 폐기 |

### 4.5 타지점 건은 차량번호 칸에 지점이 들어가 있다

`부산-조순제`, `광양-김석진`, `인천사무소`, `의왕-이건우` 형태로 **411행**.
이 건들은 각 지점에서 실적을 잡으므로 **평택 실적에 없는 게 정상**이다.

→ **지점 / 담당 / 차량을 3개 필드로 분리**한다. 나중에 타지점 실적을 받으면 그대로 붙는다.

### 4.6 한 표에 다른 업무가 섞여 있다

운송 / 계근(`공만차계근`) / 부대비용(차량번호 없이 청구만) / 셔틀이 구분 없이 들어있다.
→ **업무구분 필드 신설**. 안 나누면 건수 통계가 오염된다.

---

## 5. 마스터 데이터 (바로 확정 가능)

### KYSS에서만 얻을 수 있는 것 ★

배차일보는 `7281`, `청명`처럼 축약해서 쓴다. **전체 차량번호는 KYSS에만 있다.**

| 마스터 | 건수 | 출처 |
|---|---|---|
| 차량 (전체번호) | 68 (자차 24 / 협력사 44) | KYSS |
| 기사 | 55 | KYSS |
| 매입처 | 50 | KYSS |
| 청구처 (정식명) | 63 | KYSS |

```
7281 → 서울98바7281  (장기용차)
청명 → 경기98바7532 / 청명98사6000  [주식회사 청명로지스]  (협력사용차)
```

### 배차일보에서 얻는 것

| 마스터 | 건수 | 비고 |
|---|---|---|
| 선사 | 50 | 코드화 가능 |
| 상차지 | 60 | `동부두`·`아이포트`에 82% 집중 |
| 하차지 | 68 | 상동 |
| 규격 | 8 | 정리 완료 |
| 샤시 | 164 | 변동 있음, 아래 참조 |
| 화주 | 647 | **정리 필요** |

### 차량 뒷4자리 중복 (자동입력 설계에 직결)

**자차 24대는 뒷4자리가 전부 고유.** 협력사에서만 3쌍 중복.

| 뒷4자리 | 차량 |
|---|---|
| 1111 | 대호11사1111 / 바로1111 |
| 1231 | 삼익평택11사1231 / 티지11사1231 |
| 7542 | 경기98바7542 / 부산99사7542 |

→ 자차면 즉시 확정, 협력사 중복이면 후보 선택.
→ 일보 자차 25종 중 24종이 KYSS에서 확인됨. `3603`만 미확인(7월 건, 미청구 추정).

### 용차사 → 차량 매핑

일보 용차사 57종 중 31종이 KYSS 차량과 매핑됨. 나머지 26종은 타지점.

```
백야  → 백야99바1007 / 부산98사2664 / 부산99바7537   (주)백야물류
다원  → 경기98바7533 / 다원99바3578                 (주)다원로직스
성창  → 경기98바7536 / 성창12사1211                 성창종합물류(주)
```

`인트`=`인트란스`, `메가`=`메가로지스틱` → 별칭으로 묶을 것.
→ **용차사를 고르면 그 회사 차량 목록이 뜨는 2단 구조**로 설계.

### 샤시 — 차량 고정 아님 (사용자 확인)

차량 43대 : 샤시 164종. 주력 차량 한 대가 20종 안팎을 돌려 쓴다.
단, **주력 샤시 하나가 33~70%를 차지**한다.

```
7281  총201건  샤시 27종   6312이 140건(70%)
2637  총212건  샤시 21종   5129가 135건(64%)
6424  총205건  샤시 22종   8474가  68건(33%)
```

→ 자동 확정 금지. **차량 선택 시 최근 사용 샤시 3~4개를 버튼으로** 제시.
→ 샤시 `6621`은 9대가 공유. 차량에 고정 결합하면 안 됨.
→ **같은 날 같은 샤시가 두 대에 잡히면 경고** (물리적으로 불가능 → 오타 검출).

### 화주 정리 — 가장 손이 많이 가는 부분

일보 647종 vs KYSS 378종. 교차검증 2,354행 기준 **일치율 90.0%**.

불일치 유형:

| 유형 | 예 | 건수 |
|---|---|---|
| 앤/엔 표기 | 마운트지앤디 ↔ 마운트지엔디 | 109 |
| 법인격 유무 | 용산아산 ↔ (주)용산아산공장 | 45 |
| 오타 | 핸즈식스 ↔ 헨즈식스 / 루오지앤 ↔ 루오지엔 | 소수 |
| 공장명 vs 법인명 | 칠서공장 ↔ 삼일씨엔에스 / 빅퍼스 ↔ 현대케피코 | 소수 |

→ **KYSS 명칭을 정식명, 일보 명칭을 별칭**으로 등록.

**중요 — 화주 정리를 오픈 선행조건으로 두지 말 것.**
647종을 다 정리하려면 언제 시작할지 모른다. 마스터에 없으면 "신규"로 받아 저장하고,
미매칭 목록을 별도 화면에 쌓아 운영하며 병합한다.

### 지역+화주 검색은 이미 실무 관행이다

`청주SDI`, `울산SDI`, `천안SDI`, `김해SDI`, `경산SDI`, `용인SDI`, `광주SDI`
→ 새 규칙이 아니라 **기존 관행의 시스템화**. 적응 비용 거의 없음.

**주소가 하나뿐인 화주가 77.5%** → 화주명만 치면 바로 채워진다.
사업장 선택이 필요한 건 22.5%뿐 (이나바 24개, 두진양행 20개, 알톤스포츠 15개 등).

지역 별칭은 **손으로 넣어야 한다.** 오창은 행정구역상 청주지만 실무에선 "오창"이라 부른다.
주소에서 자동 추출하면 이런 게 다 누락된다.

---

## 6. 컨테이너번호 검증 (ISO 6346)

`소유주 3자 + 장비구분 1자(U/J/Z) + 일련번호 6자리 + 체크디짓 1자`

### 실측 성능

- 원장 6,780건 중 **검증 통과 6,777 / 실패 3 / 형식오류 2** → 실패율 0.04%
- 무작위 1자 오타 4,000회 주입 시 **검출률 96.9%**
- **자동 교정은 하지 말 것.** 후보가 1개로 좁혀지는 경우는 31%뿐, 중앙값 6개

### 표시 4단계

| 상태 | 표시 |
|---|---|
| 10자 이하 | 회색, 조용히 대기 (타이핑 중 경고 금지) |
| 11자 · 통과 | 초록 + ✓ → 자동으로 다음 칸 포커스 이동 |
| 11자 · 체크디짓 불일치 | 빨강 + "번호를 다시 확인하세요" |
| 형식 불일치 | 주황 + "영문 4자 + 숫자 7자" |

### 확실하게 잡히는 것

- **4번째 자리는 U/J/Z만 가능.** 6,780건 중 6,779건이 U. 유일한 예외 `DYLY`가 오타였고 교정 후보도 1개로 특정됨
- **혼동 문자 자동 변환** (자리로 판별되므로 애매함 없음, 바꾼 자리는 노란색 깜빡임)
  - 숫자 자리: O→0, I·l→1, S→5, B→8
  - 문자 자리: 0→O, 1→I, 5→S, 2→Z
- 소유주 코드 167종 (SKHU 836, SKLU 602, HLHU 450... 상위 10개가 53%) → 처음 보는 코드면 경고만, 차단 금지

### 파싱에 활용

메신저·D/O에서 영문4+숫자7 패턴을 뽑았을 때 **체크디짓까지 맞으면 컨번호로 확정**해도 된다(우연 확률 1/11).
틀리면 노란색 경고 → 사람이 확인.

### 컨번호 중복 판정 3단계

| 조건 | 판정 | 동작 |
|---|---|---|
| 같은 날 · 같은 차 · 수출입 반대 | 왕복 | 통과 |
| 같은 날 · 다른 차 / 금액 한쪽 없음 | 분할·부대비용 | 확인만 |
| 30일 초과 간격 | 재사용 | 통과 |
| 같은 날 · 같은 차 · 수출입 동일 · 금액 동일 | **중복 입력 의심** | 빨간 경고 |

---

## 7. 데이터 모델 (초안)

### `dispatches/{id}`

```
배차번호(자체 발번)   ← 유일키
업무구분             운송 / 계근 / 부대비용 / 셔틀
지점                 평택 / 부산 / 인천 / 광양 / 의왕 / 울산 ...
담당                 (타지점 건의 담당자)

날짜, 시간, 수출입(I/O)
차량구분             자차 / 용차
차량번호(전체), 차량ID, 용차사, 기사명, 기사연락처
샤시번호             (선택, 최근 사용 3~4개 추천)

화주ID, 화주명(값 복사), 사업장, 주소(값 복사), 담당자[]  ← {이름, 직급, 번호}
청구처ID, 청구처명(값 복사)
선사, 상차지, 하차지

사이즈(20/40/45), 타입(D/H/RE/FR/OT)
BL본체, BL접미사, DO, 컨번호[]
자가여부(bool), 통관여부(bool), 실봉인번호
테어(숫자), 메모(구 보세근거번호)

청구예정, 하불예정          ← 앱이 관리
청구확정, 하불확정, 확정일   ← KYSS에서 읽어옴 (읽기전용)

비고, 기타
createdAt, createdBy, updatedAt, updatedBy
```

**설계 원칙**

- **금액은 예정/확정을 분리.** 같은 칸에 두면 언제 뭐가 맞는지 알 수 없다.
- **마스터 값은 복사해서 저장.** ID만 링크로 걸면 화주가 이사했을 때 **작년 배차 기록의 주소까지 바뀐다.** 정산·분쟁 때 "그때 그 주소"가 남아야 한다.
- **컨번호는 배열.** 계근 건은 실제로 여러 개다.
- **담당자도 배열.** 지금 전화번호 칸에 `함지만 과장님 031-678-0546 / 최수안 주임님 031-678-0580`처럼 뭉쳐 있다(839종). 폰에서 번호 탭해서 걸리게 하려면 분리가 전제.

### 마스터 컬렉션

```
vehicles/{id}       전체번호, 뒷4자리, 차량구분, 소속(자차/용차사), 기사, 연락처, 톤수,
                    장거리가능(bool), 45피트가능(bool)   ← Sheet1/Sheet3에 흩어져 있던 것
carriers/{id}       용차사 (별칭 배열)
drivers/{id}        기사 55명
chassis/{id}        샤시 164
shippers/{id}       화주 (별칭 배열) + 하위 sites/{id} (사업장별 주소·담당자)
billTo/{id}         청구처 63
vendors/{id}        매입처 50
lines/{id}          선사 50
terminals/{id}      상하차지 68
users/{uid}         이름, 소속지점, 역할(admin/editor/viewer), 금액열람권한
```

---

## 8. 화면 구성

**① 배차대장** (기본) — 날짜 범위 + 검색창, 열 머리글마다 엑셀식 체크박스 필터. PC는 표, 폰은 카드.
**② 입력** — 수동 / 메신저 붙여넣기 / 파일 드롭이 전부 여기로.
**③ 요약** — 차량별·날짜별·화주별·청구처별. **현재 필터 상태 그대로 집계**.
**④ 마스터** — 차량·용차사·화주·청구처 등 관리.

### 필터·요약 설계

- 날짜 범위로 잘라 한 번에 받고 **메모리에서 처리**. Firestore 쿼리로 매번 걸면 느리고 읽기 요금만 든다.
- 자주 쓰는 조합은 "내 필터"로 저장 (예: 평택/미완료)
- **필터된 것만 엑셀 내보내기** — 정산 때 제일 많이 쓴다. `하불`·`마감자료` 레이아웃을 프리셋으로.
- 폰은 카드 리스트로 자동 전환. 표를 가로 스크롤하면 실무에서 못 쓴다.
- Firestore 오프라인 캐시 켤 것 (현장 신호 약함)

### 입력 파이프라인

```
붙여넣기 / PDF·이미지 드롭 / 수동
        ↓
정규식 1차 (컨번호·차량번호·날짜·전화·B/L)   ← 정확하고 공짜
        ↓
Claude API 2차 (상하차지·화주·비고 등)      ← 흐물흐물한 것만
        ↓
마스터 정규화 (화주→정식명, 차량→뒷4자리 매칭)
        ↓
   ★ 검토 화면 ★   확신 높음=검정 / 애매=노랑
        ↓
      Firestore
```

**수동 입력창은 자동 확정 OK, 파싱 결과는 자동 확정 금지.** 이 구분이 핵심이다.
메신저에 `3456`만 있으면 차량인지 시간인지 금액인지 알 수 없다.

**OCR은 Tesseract 쓰지 말 것.** 이전에 정확도 문제를 겪었고, Claude에 이미지를 그대로 넘기는 쪽이 한글 D/O에서 비교가 안 된다.

### 저장 전 경고 규칙

| 규칙 | 근거 |
|---|---|
| 컨번호 체크디짓 불일치 | 검출률 96.9% |
| 같은 날 같은 샤시 2대 배정 | 물리적 불가 |
| 컨번호 중복 (위 3단계 판정) | 재사용 573건 중 진짜 의심만 골라냄 |
| 날짜는 선택만 허용 | `-`, `2/23` 같은 값 재발 방지 |

---

## 9. KYSS 연계

### 알아낸 것

- **API 오픈** (사용자 확인). 계정은 `.env`로 별도 전달 예정
- **API 분석 완료 → `kyss-api/` 에 보관.** 프론트 Vue SPA 청크 전수 분석으로
  전 메뉴·화면별 서버 API를 복원했다. (README·전체맵·API인덱스·`out/*.json`)
  - 최상위 메뉴 6 (TMS/CFS/공통기준정보/시스템/법제도이행관리/고객서비스관리),
    메뉴 181개, 화면모듈 528개, **API 엔드포인트 1,333개**, 호출정의 1,770개
  - 계정 기준: 안계문 / 국양로지텍(주) — 이 계정에 보이는 메뉴만 포함
- 사무소 9개: 본사, 서울지사, 영남지사, 전남지사, 울산사무소, 인천사무소, 의왕사무소, 평택사무소, 중부사무소
- **실적 조회는 `청구일자` 기준** (기존 `kyss-siljeok-download` 스킬에서 확인)
  → **7월 실적이 35건뿐인 이유가 이것.** 7월 배차 774건 중 청구된 건만 잡힘
- `월별&업체별 실적현황`과 `월별&업체별 매출현황`은 별개 메뉴 (혼동 주의)

### 우리가 쓸 핵심 엔드포인트 (모두 `POST`, JSON) ★

| 화면 | 엔드포인트 | 용도 |
|---|---|---|
| 실적통합관리 `OP_100010` | `/api/outputs/selectTotalOutputsList` | **행단위 실적** (우리가 받은 `실적통합관리` 엑셀의 원본) |
| 월별&업체별 실적현황 `OP_140020` | `/api/outputs/statssales/selectMonCorpOutsList` | 집계 실적 (스킬이 받던 화면) |
| 월별&업체별 매출현황 `OP_140010` | `/api/outputs/statssales/selectMonCorpSalesList` | 집계 매출 (별개 메뉴) |
| 통합 오더 조회 `OP_100020` | `/api/orderList/selectTotalOrderList` | 오더 목록 |
| 로그인 | `/api/login` → `/api/session` / `/api/menuMap` | 세션·메뉴 |

**`selectTotalOutputsList` 검색 파라미터** (실적통합관리):
`BRANCH`(사무소) · `DATE_FM`/`DATE_TO` · `OP_SCH_DATE_TYPE`(기본 `BILL_CF_DT`=청구확정일) ·
`ORDER_DS_DIV`(오더구분, 기본 `"D"`=도어) · `ORDER_TYPE`(수출입) · `BILL_CORP`(청구처) ·
`EXC_SCH_TYPE`(기본 `"B"`) · `SEARCH_TYPE`(기본 `"W"`) · `KEYWORD` 등
→ **`OP_SCH_DATE_TYPE=BILL_CF_DT` 기본값이 "실적=청구일자 기준"의 근거.** 배차일자 기준으로 받으려면 이 값을 바꿔야 한다.
→ **`ORDER_DS_DIV="D"`(도어)가 기본이라 도어만 담겼던 것.** 계근·셔틀은 다른 구분 코드로 조회해야 잡힌다.

### 인증 방식 (확인됨 — 중계 함수 설계 근거)

- **세션 쿠키 기반.** 토큰 헤더 아님.
  1. `POST /api/login` (username/password) → 세션 쿠키 발급
  2. 이후 `POST /api/auth/authorized`(로그인여부 `true/false`), `POST /api/session`(계정정보),
     `POST /api/menuMap`(권한별 메뉴)로 세션 확인
  3. 조회 API는 모두 `POST` + JSON 바디, 쿠키로 인증
- 미인증 상태로 조회 시 **401**. → Cloud Function이 로그인→쿄키 보관→만료 시 재로그인 중계.
- 권한 예: 계정 `authorities` = `KYLT_E`, `KYLT_M`, `KYLT_SM` (조회/실적 권한 보유)
- 진단 스크립트: `kyss-api/kyss_login_diag.py` (비밀번호는 로그에서 마스킹).

### 남은 확인 사항 (실호출 필요 — `.env` 받은 뒤)

| 질문 | 상태 |
|---|---|
| API가 등록까지 되나, 조회만 되나 | 소스상 `save*`/`delete*` 함수 **존재**(등록 가능). 단 분석 시엔 조회만 호출함. 우리 앱은 **조회만 쓸 계획** |
| API 응답의 `청구합계`도 총액 반복 구조인가 | **엑셀(=이 API 출력)에서 이미 반복 확인됨(4.1).** 실호출 응답으로 최종 확정 필요 |
| `구분`(`ORDER_DS_DIV`) 코드에 도어 말고 뭐가 있나 | 기본 `"D"`=도어 확인. 나머지 코드값은 실호출 응답/코드테이블로 확인 |
| 인증 방식(세션/토큰), 호출 제한 | **세션 쿠키로 확인 완료.** 호출 제한(rate limit)은 실호출로 확인 |

### 대사 키

**컨번호 + 날짜(±3일)**가 유일한 공용 키다.
- 컨번호 일치율 97.7%
- BL 일치율 98.0% — 불일치 47건은 대부분 표기 문제(일보가 `...336A`, `...336B`처럼 컨테이너별 접미사를 붙임). **BL 본체와 접미사를 나누면 거의 100%**
- 차량은 일보가 뒷4자리, KYSS가 전체번호 → 뒷4자리로 매칭. 용차는 회사명 ↔ 차량번호 접두어/매입처로 매칭

---

## 10. 진행 순서

| 단계 | 내용 | 비고 |
|---|---|---|
| **1** | **마스터 확정** | 앱과 무관하게 지금 시작 가능. 엑셀로 뽑아 확인받기 |
| **2** | Firebase 세팅 | 프로젝트 생성, Firestore 스키마, Auth, `.env`+`.gitignore` |
| **3** | 조회 화면 (읽기 전용) | 2026년 5,935건 이관. 2주간 엑셀과 병행 |
| **4** | KYSS 연계 | Cloud Functions, Blaze 전환. **API가 열려 있으므로 앞당김** |
| **5** | 입력 화면 | 차량 뒷4자리, 화주+지역, 컨번호 검증. 엑셀 중단 |
| **6** | 파싱·OCR | 메신저 붙여넣기, D/O 읽기 |

**조회 먼저, 입력 나중.** 사람들이 화면에 익숙해진 뒤에 입력을 옮겨야 저항이 없다.
API가 열려 있으므로 4단계를 3단계 직후로 당기면, 입력 기능이 없어도 앱을 쓸 이유가 생긴다.

---

## 11. 아직 안 받은 자료

- [ ] KYSS 계정 (`.env`)
- [ ] 타지점 실적 (부산·인천·광양·의왕·울산) — 411행 대사용
- [ ] 메신저 원문 10~20개 (지저분할수록 좋음) — 6단계 전 필수
- [ ] D/O 샘플 1~2장
- [ ] 화주별 지역 별칭 (오창·반월·향남 등 실무 호칭)

---

## 12. 파일

```
CONTEXT.md                           이 문서 (세션 시작 시 먼저 읽음)
.env.example                         KYSS 로그인 등 환경변수 틀 (실제 .env 는 gitignore)
.gitignore                           .env / 프로필 / bundle / *.xlsx 제외

kyss-api/                            ★ KYSS API 전수 분석 (리포 커밋)
  README.md                          조사 방법·규모·재조사법
  KYSS_전체맵.md                      메뉴 트리 + 화면별 API 표 (주 산출물)
  KYSS_API_인덱스.md                  엔드포인트 1,333개 전수 인덱스 + 역참조
  KYSS_메뉴외화면.md                  메뉴 미노출 화면 383개
  kyss_login_diag.py                 로그인 진단 스크립트 (비번 마스킹)
  out/*.json, menu_tree.tsv          기계조회용 명세 (검색파라미터·필드라벨 등)
  ※ 제외(커밋 안 함): .kyss_diag_profile/(세션쿠키 44MB), bundle/(JS 18.8MB), diag/(PII 로그)

data/  (엑셀은 gitignore, 별도 관리)
  26년배차일보.xlsx                  원본
  26년배차일보_정리.xlsx              정리본 (2025년 포함, 정리내역 시트 있음)
  실적통합관리_통합정리.xlsx           KYSS 실적 통합본
  배차일보_실적_대사_2026.xlsx         일보↔실적 대사 결과
  실적통합관리_2026-07-27.xlsx        KYSS 실적 2,371행
  실적통합관리_2026-07-27__1_.xlsx    KYSS 실적 40행 (위와 교집합 0)
```

---

## 13. 작업 로그

| 날짜 | 내용 |
|---|---|
| 2026-07-27 | 배차일보 분석·정리, KYSS 실적 2파일 대사, 마스터 후보 추출, 이 문서 작성 |
| 2026-07-27 | KYSS API 전수 분석본(`kyss-api/`) 리포 반영. 인증(세션 쿠키)·핵심 엔드포인트·실적통합관리 검색 파라미터 확정. §9 갱신. `.env.example`/`.gitignore` 추가 (실제 `.env`는 나중에) |

> 세션 종료 시 위 표에 한 줄 추가하고, 바뀐 결정은 본문에 반영한다.
